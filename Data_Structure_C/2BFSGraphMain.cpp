//#include <stdio.h>
//#include "2BFSGraph.h"
//
//int main(void) {
//	Graph graph;
//	GraphInit(&graph, 10);
//
//	AddEdge(&graph, A, B);
//	AddEdge(&graph, A, H);
//	AddEdge(&graph, A, I);
//	AddEdge(&graph, A, J);
//	
//	AddEdge(&graph, A, D);
//	AddEdge(&graph, B, C);
//	AddEdge(&graph, D, C);
//	AddEdge(&graph, D, E);
//	AddEdge(&graph, E, F);
//	AddEdge(&graph, E, G);
//
//	ShowEdgeInfo(&graph);
//
//	BFSShowVertexInfo(&graph, A);
//
//	GraphDestroy(&graph);
//}